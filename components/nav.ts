import styled from 'styled-components';

const Nav = styled.nav`
    backdrop-filter: saturate(180%) blur(20px);
    display: flex;
    height: 36px;
    padding: 8px 2px;
    align-items: center;
    border-radius: 10px;
`;

export default Nav;
