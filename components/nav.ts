import styled from 'styled-components';
import {motion} from 'framer-motion';

const Nav = styled(motion.nav)`
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
`;

export default Nav;
