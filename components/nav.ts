import styled from 'styled-components';

const Nav = styled.nav`
    display: flex;
    height: 36px;
    padding: 8px 2px;
    align-items: center;
    border-radius: var(--radius);
`;

export default Nav;
