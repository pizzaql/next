import {styled} from 'linaria/react';

const Footer = styled.div`
    display: flex;
    flex-direction: column;
    width: 100%;
    margin-top: 40px;
    align-items: center;
    color: var(--text);
    font-size: 14px;
    opacity: 0.5;
    text-align: center;
`;

export default Footer;
